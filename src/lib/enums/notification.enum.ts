export enum NotificationType {
  LEAVE = 'LEAVE',
  PAYROLL = 'PAYROLL',
  ATTENDANCE = 'ATTENDANCE',
  LIFECYCLE = 'LIFECYCLE',
  SYSTEM = 'SYSTEM',
}

export enum NotificationPriority {
  LOW = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH',
  URGENT = 'URGENT',
}
